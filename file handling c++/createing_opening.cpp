#include<bits/stdc++.h>
#include<fstream>
using namespace std; 

int main()
{
    fstream obj;
    //ios::trunc opens the file
    //in mode if stream, out mode with trunk in ofstream ,app mode, ate mode, binary file
    obj.open("chetan.txt",ios :: in | ios::out | ios::trunc);
      
}